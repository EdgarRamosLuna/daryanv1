import Layout from "@/components/client/Layout";

export default function Reports(){
    return (
       <>
            Reportes del bicho
       </>
    )
}
Reports.getLayout = (page) => <Layout>{page}</Layout>;