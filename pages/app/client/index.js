import Layout from "@/components/client/Layout";

export default function ClientDashboard(){
    return (
       <>
            Dashboard Client De la Bicha
       </>
    )
}
ClientDashboard.getLayout = (page) => <Layout>{page}</Layout>;